# 训练记录 20260827-174327（档 A 基准）

> 权重本身在 `finetune/outputs/20260827-174327/`，被 `.gitignore` 排除，
> 不进仓库。**本文件是它在仓库里的唯一凭证**，含全部可核对的指纹。
> 评测结果见 `docs/m3-微调效果对比分析报告.md` §11.6。

> **服务器在下载完 adapter 后失联**，`train-stats.json` 与 `train.log`
> 未能取回。本文件为当时从服务器读出的数值的本地存档。
> **adapter 权重已完整下载并逐文件 md5 校验一致**，可正常使用。

## 配置

```
基座      Qwen2.5-VL-3B-Instruct  (/root/autodl-tmp/qwen3b)
LoRA      r=16  alpha=32  dropout=0.05
目标层    q_proj k_proj v_proj o_proj gate_proj up_proj down_proj
精度      bf16
max_pixels 802816  (896×896)
优化器    adamw_8bit
存点      每 50 步，save_only_model=True
数据      train 1783 / val 375
          md5 6045cd464a1aa1446866072561a6a8c3  train.jsonl
          md5 7713ab81380a3bc24bd689ce1059cd79  val.jsonl
硬件      RTX 4090 24GB
代码      commit 3222e33
```

## 结果

```
steps        446
train_sec    5581.9   (93 分钟)
peak_vram    7458.1 MB
error        ''（空）
可训练参数   37,152,768 / 2,071,177,216 = 1.7938%
loss         0.5607 (step 10) → 0.2111 (step 446)，44 个记录点
adapter      348 个 lora_B 全部非零
adapter_model.safetensors  md5 48a0e5cb21fd793bc42e56d0ed267aea
```

## loss 轨迹（每 10 步）

```
0.561 0.378 0.332 0.289 0.286 0.228 0.316 0.345 0.298 0.280
0.264 0.298 0.255 0.269 0.247 0.267 0.292 0.173 0.227 0.254
0.217 0.289 0.243 0.162 0.267 0.229 0.227 0.252 0.232 0.196
0.210 0.229 0.241 0.209 0.242 0.214 0.175 0.207 0.197 0.210
0.269 0.190 0.185 0.211
```

## 与上一轮（20260827-013143，已作废）的关系

上一轮因两个缺陷作废：`SYSTEM_PROMPT` 只列 4 个鼠标动作、
标签掩码偏移 3 个 token。本轮修复了这两处，其余完全一致
（同一份数据、同一组超参）。

**但 loss 曲线区分不出这两轮：**

| | 旧轮（作废） | 本轮 |
|---|---:|---:|
| step 10 | 0.6080 | 0.5607 |
| 末点 | 0.1996 | 0.2111 |
| peak_vram | 7118.4 | 7458.1 |

**所以报告里不能用 loss 论证修复生效。** 掩码修复的证据是结构性的：

1. 训练日志中「找不到 assistant 起始标记」告警数 = **0**
2. 本地单元测试 `TestLabelMasking`（3 条）直接断言掩码位置
3. 开训前代码级自检确认锚点为 `<|im_start|>assistant\n`

两轮的真实差别只能靠 `eval.action` 与端到端任务测出来。

## 未完成

- [ ] `python -m eval.action` 跑 375 条验证集（新口径指标）
- [ ] 客机端到端 5 个任务（唯一能与 17% 旧基线干净对比的路径）
- [ ] 更新 `docs/m3-微调效果对比分析报告.md` §2/§4/§8/§9/§10
      —— **旧数标注口径原样保留，不就地改**
