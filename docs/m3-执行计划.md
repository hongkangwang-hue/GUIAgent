# M3 执行计划（2026-08-24 起草，M2 实测数据驱动）

> 周期 **2026-09-14 ~ 09-20**。本文是 `milestone/milestone-3-benchmark-finetune.md`
> 的执行版：里程碑文档定义「做什么、验收什么」，这份定义「哪天做、先做什么、
> 卡在哪里」。两者冲突时以里程碑文档为准。

---

## 0. 先纠正一处

M2 实测暴露了三个失败模式（零动作报完成、连续重复动作、拆解粒度与应用行为
错配），很容易顺手把它们排进 M3。**不行。** `milestone-3` 的「明确不做」第 9 条：

> 不做鲁棒性与错误恢复相关工作，留到 M4。

M3 是**微调与横评**，M4 才是鲁棒性。M2 的失败数据在 M3 里只有一个合法用途：
**当提示词消融的对照基线**（任务 3）。真正的修复归 M4。

---

## 1. 开工前必须清掉的三件事

这三件都在 M2 期间做，**任何一件拖到 09-14 当天都会连锁阻塞整周**。

### ① 前置件 ② ③ —— 需要 GPU，还没开始

| 前置件 | 内容 | 为什么不能拖 |
|---|---|---|
| ② | LoRA 100-step smoke test | 训练脚本跑不通的话，D1 晚间启动的挂机训练会空跑一夜 |
| ③ | 3B 4-bit 真实分辨率单图推理 + 峰值显存实测 | D1 下午的本地部署直接沿用这个 `max_pixels` 配置；没有它就得当天现测 |

两项都要显卡。**本机 Blackwell 工具链若仍未打通，就短租一台 3-4 小时一次做完**，
顺带把 M3 任务 6 的短租内容合并进去。

### ② 训练数据缺口定案 —— 须在 M2 结束前

实测可用 **569 条训练样本**，大纲定的是 3000-4000，缺口约 2400。两条路：

- **(a) 补充来源**把池子做到 3000-4000（候选见 `m3-prereq/README.md`）
- **(b) 就用 569 条**，在报告里说明数据受限对结论的影响

选 (b) 时「微调无显著提升」是**可预期的合法结论**（降级路径 2）。
但这个决定要现在做，不能等到看见 loss 曲线难看再补说明——那就成了事后找补。

### ③ 填单价 —— 五分钟的事，挡住两项验收

`.env` 里 `DASHSCOPE_PRICE_IN_PER_1K` / `OUT_PER_1K` 为空时 `CostInfo.priced=False`，
所有成本数字是 0。影响：

- M2 验收 11「单任务真实成本实测」拿不到数据
- M3 交付物 8 的模式 A/B 成本对比、任务 9 的「成本与延迟权衡」图无法作图
- **成本熔断（`cost_limit_cny`）不生效**——拿恒为 0 的成本比上限等于没有保护

已有轨迹的 token 数都落盘了，填上单价可直接回算，**不必重跑**。

---

## 2. 七天排期

两条线并行：挂机线占日历不占人力，人力线必须给整块时间。

| 日 | 挂机线 | 人力线 | 产出 |
|---|---|---|---|
| **D1** 09-14 | 上午构建微调数据集 → **晚间启动 LoRA** | 下午 3B 本地部署，实现 `QwenVLLocalBackend` | 显存 / 延迟 / 冷加载实测 |
| **D2** 09-15 | LoRA 训练中；提示词消融 P1 开跑 | 上午横评 API 后端（2h）→ 下午短租 3-4h | GLM-4V / Llama 实测 |
| **D3** 09-16 | 训练收尾；消融 P2 | **`LocalVLMGrounding` 实现（整天）** | — |
| **D4** 09-17 | 消融 P3；ScreenSpot 档 1/2 批量跑 | **`LocalVLMGrounding` 接入与联调（整天）** | 模式 B 可用 |
| **D5** 09-18 | ScreenSpot 档 3-6 | **模式 B 接入截止线** —— 未跑通即执行降级路径 3 | 横评六档数据 |
| **D6** 09-19 | — | 模式 A/B 端到端对比（5 任务 × 3 次 × 2 模式 = 30 轮） | 对比数据 |
| **D7** 09-20 | — | 写报告、出图、验收 PPT | 交付物 10-12 |

**关键路径是 D3-D4 那两天。** `LocalVLMGrounding` 是本周唯一「决定微调成果是否
承重」的东西，其余都能降级。D5 收盘时它没跑通就按降级路径 3 停手，**不要往
D6-D7 借时间**——那两天要交报告。

---

## 3. M2 实测给 M3 的四条约束

这四条都是这周实测出来的，不是推测。

### ① 消融实验的环境必须先清干净，否则结论是噪声

M2 的对照数据：**同一份代码、同一个模型、只改环境**——

| | 脏环境 | 清场后 |
|---|---|---|
| 成功率 | 44% | **75%** |
| 零动作报完成 | 49% | **0%** |

**环境差异带来的波动（31 个百分点）远大于三个提示词版本之间的预期差异。**
消融跑 45 次执行，中间只要混进一次记事本会话恢复或一个卡住的模态框，
P1/P2/P3 的排序就没有意义。

**所以 M3 任务 3 开跑前，必须确认 `tasks/reset_desktop.py` 在客机上生效**，
并在消融报告里记录每一版跑的是同一套 reset。

### ② 消融的样本量 n=15/版本 撑不起强结论

同一个任务（`发送消息`）在四次测量里给出 **0/5、1/5、5/5、0/5**。
在这种方差下，n=15 分不出 P2 比 P1 好还是运气好。

验收标准 9 已经要求「说明 n=15/版本 的样本量对结论强度的限制」——**照做，
并且分任务报**，不要只给一个总成功率。分任务能看出「P2 只在多步任务上有用」
这类有信息量的模式，总成功率会把它抹平。

### ③ 模式 B 在这 5 个任务上大概率不提升成功率

M2 全部 170 步的实测：`grounding_ms` 合计 **0.2ms**，来源全是 `native`，
**没有一次失败是定位造成的**。失败全在规划层（提前报完成、重复动作、
拆解错配）和环境层。

模式 B 换的是坐标的**来源**，换不掉规划层的毛病。所以：

- **预期收益在 token 成本，不在成功率**（送 API 的截图可降分辨率）
- 降级路径 4 已经写明「模式 B 成功率低于模式 A 不视为失败」——这条会用上
- 报告里要**主动**给出这个预期与实测的对照，而不是等数据难看再解释

`LocalVLMGrounding` 仍是 P0，因为大纲 W5 要求微调成果在系统里承重。
**做，但对它的期望要摆正。**

### ④ 端到端对比要沿用 M2 的 `precondition` 机制

M2 加的起点检查（reset 之后、Agent 之前验一次环境）在实战里拦下了 5 轮
无效数据——消息程序没开时，那 5 轮本会被记成 0/5 失败，把总成功率从
75% 拉到 60%，而且从数字上完全看不出来。

M3 任务 9 的 30 轮对比、任务 3 的 45 次消融，**都要带这个机制**。

---

## 4. 里程碑文档需要改的地方

以下是 M2 实测查出的、与现有文档不符或有缺口的地方。

### M3

| 位置 | 问题 | 建议 |
|---|---|---|
| 任务 3 提示词消融 | 没写评测环境要求 | 补一句：45 次执行须在同一套 reset 下进行，并沿用 `precondition` |
| 任务 9 模式 A/B 对比 | 同上 | 同上 |
| 设计思路 · 模式 B | 收益预期只写了「成本收益大/小」 | 补 M2 实测依据：grounding 不是当前瓶颈，预期收益在成本不在成功率 |

### M4

| 位置 | 问题 | 建议 |
|---|---|---|
| 任务 1 · E1-E8 预期分类 | **缺一类 M2 实测到的高频错误** | 补 **E9 计划-应用错配**：Planner 拆出的子任务与应用实际行为不匹配。实例：`mock_messenger` 的输入框回车即发送，而计划里还有一个独立的「点击发送按钮」子任务，导致每轮重复发送 |
| 任务 1 · E4 状态误判 | 定义偏窄 | M2 实测的形态是「thinking 明确写着子任务未达成，输出仍是 `done:true`」——**推理正确而输出字段错误**，不是判断错误。检测手段应针对输出约束，不是感知增强 |
| 设计思路 · Reflector 第一级 | OpenCV 帧差 | M2 数据支持这一级：连续重复动作占比可观，帧差能直接拦下「点了没反应还接着点」 |

### M5

| 位置 | 问题 | 建议 |
|---|---|---|
| 任务 · 「逐个验证重置脚本能把环境恢复到一致初始状态」 | **M2 实测证明任务级 reset 做不到** | 60 轮正式评测改用**虚拟机快照恢复**。实测撞到四类 `taskkill` 够不着的残留：记事本会话恢复（状态在磁盘）、开始菜单/搜索浮层（系统 UI）、帐户登录模态框、上一轮遗留窗口 |
| 验收标准 9 | **与已记录的合规偏差直接冲突** | 现文写「全部评测任务、截图样本与演示视频均来自预置的测试环境，**不含真实个人账号与个人数据**（M1 建立的干净快照）」。而 `执行计划.md` §3 记录：客机登录了真实微软账号、OneDrive 同步个人文件，用户已决定不清理。**这条验收标准按现状无法通过，必须二选一**：清理客机重拍快照，或改写为执行计划里给出的准确表述 |

---

## 5. 挂起的决定

1. **训练数据缺口选 (a) 还是 (b)** —— M2 结束前定案
2. **全模型本地部署 vs 仅微调模型本地部署** —— M0 需确认事项，问导师
3. **M5 验收标准 9 的合规表述** —— 见上表，需要用户拍板
