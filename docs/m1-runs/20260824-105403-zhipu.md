# M1 D1：多模态 API 连通性验证

> 采集时间：2026-08-24 10:54
> 测试图片：现场截屏 1920×1080

验证两件事：① 模型能否看懂真实桌面截图并输出可解析的结构化结果；② `ChatOpenAI` + 平台 OpenAI 兼容 `base_url` 能否正确传图。

## 结果汇总

| 平台 | 模型 | 状态 | 可解析 | 坐标在范围内 | 平均延迟 | 平均 tokens | 限流重试 |
|---|---|---|---|---|---|---|---|
| 智谱开放平台 | glm-4.6v | 通过 | 3/3 | 3/3 | 24443ms | 1441 | 0 次 |

## 逐家详情

### 智谱开放平台

- 模型：`glm-4.6v`
- 端点：`https://open.bigmodel.cn/api/paas/v4`
- 送出图片：{'sent_width': 1024, 'sent_height': 768, 'jpeg_quality': 85, 'downscale': 1.0, 'bytes': 204430}
- 累计用量：{'model': 'glm-4.6v', 'requests': 3, 'prompt_tokens': 3534, 'completion_tokens': 790, 'cached_tokens': 1189, 'total_tokens': 4324, 'cost_cny': 0.0, 'priced': False}

> 已知限制：flash 档为免费或低价模型，适合做横评的成本对照组

**第 1 次** — left_click {'x': 456, 'y': 310} | 范围内 | 11516ms | 1458 tokens

```

{"action": "left_click", "x": 456, "y": 310, "thinking": "屏幕最上方的窗口是Windows PowerShell窗口，其标题栏位于窗口顶部，选择标题栏内的一个坐标进行点击，该坐标在窗口标题栏范围内，符合任务要求。"}
```

**第 2 次** — left_click {'x': 455, 'y': 309} | 范围内 | 17512ms | 1421 tokens

```

{"action": "left_click", "x": 455, "y": 309, "thinking": "屏幕最上方的窗口是Windows PowerShell窗口，其标题栏位于窗口顶部区域，选择标题栏内合适的坐标进行点击，以完成对窗口标题栏的交互操作。"}
```

**第 3 次** — left_click {'x': 456, 'y': 307} | 范围内 | 44302ms | 1445 tokens

```

{"action": "left_click", "x": 456, "y": 307, "thinking": "屏幕最上方的窗口是Windows PowerShell窗口，其标题栏位于窗口顶部，选择标题栏内的坐标进行点击。"}
```

## 结论

此文件由 `python scripts/verify_llm.py` 生成，是 M1 D1 验收项的证据。
M2 的 `OpenAICompatBackend` 使用的正是这里验证过的消息构造方式，
三家平台共用同一实现，M3 横评时只切换配置。
